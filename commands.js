var commands = function (input) {
	var inParts = input.split(' ');
	var cmd = inParts[0]
	if(cmd === 'ls' || cmd === 'dir') {
		// List directory
	} else if(cmd === 'cd') {
		// Change Directory
	} else if(cmd === 'dl') {
		// Download File
	} else if(cmd === 'exit' || cmd === quit) {
		// Quit
	} else if(cmd === 'help' || cmd === '?') {
		// Help
	}
}